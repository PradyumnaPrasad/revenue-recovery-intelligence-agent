fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
numpy==2.1.3
pandas==2.2.3
faker==30.8.2
simpleeval==1.0.3
pyyaml==6.0.3
structlog==24.4.0
httpx==0.28.1
razorpay==1.4.2
google-genai==2.20.0
jinja2==3.1.4
pytest==8.3.3
pytest-asyncio==0.24.0
hypothesis==6.115.5
python-dotenv==1.0.1

# Deliberately not used (plan.md §1.3 / §8): alembic (create_all() + `make
# reset` — the demo always seeds from scratch, no forward migration history
# exists), apscheduler (the tick is a plain function invoked by
# /simulate/advance — reproducible, no concurrency bugs), scikit-learn (no
# trained model — see plan.md §6.4, prior_v1 is a published table).
